import React from "react";

const Input = () => {
  return (
    <div className='inputBox'>
      <input type='text' placeholder='MFLATE AMOUNT' />
      <button>ENTER</button>
    </div>
  );
};

export default Input;
